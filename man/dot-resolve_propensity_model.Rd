% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learners_registry.R
\name{.resolve_propensity_model}
\alias{.resolve_propensity_model}
\title{Resolve a propensity-model spec (used only when is_rct = FALSE). Returns a
builder producing a list with `fit(X, A, n_actions)` and `predict_proba(X)`.}
\usage{
.resolve_propensity_model(spec, seed = NULL)
}
\description{
Resolve a propensity-model spec (used only when is_rct = FALSE). Returns a
builder producing a list with `fit(X, A, n_actions)` and `predict_proba(X)`.
}
\keyword{internal}
