% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baselines.R
\name{papd}
\alias{papd}
\title{PAPD baseline (Imai's Population Average Prescriptive Difference, centered).}
\usage{
papd(
  X,
  A,
  Y,
  propensity,
  n_shuffles = 100L,
  n_folds = 3L,
  contextual_policy = "policy_tree",
  best_arm = "ips",
  reward_model = "random_forest",
  policy_features = NULL,
  n_cores = 1L,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{X}{Numeric matrix or data frame of covariates, shape (n, d).}

\item{A}{Integer vector of observed treatments, values in \eqn{\{0, \ldots,
K-1\}}. Factors are auto-coerced with a message.}

\item{Y}{Numeric vector of observed outcomes.}

\item{propensity}{Scalar, length-n vector, or (n, K) matrix. A scalar
broadcasts; a vector is read as \eqn{p(A_i \mid X_i)} for the observed
action only. Required when \code{is_rct = TRUE} (the default); ignored
when \code{is_rct = FALSE}, where the propensity is fit on the nuisance
folds and \code{propensity} may be left \code{NULL}.}

\item{n_shuffles}{Number of Algorithm-1 repetitions (each with a
different seed). Defaults to 100.}

\item{n_folds}{Number of folds \eqn{K} the data is partitioned into per
shuffle. Must be at least 2. Defaults to 6.}

\item{contextual_policy}{One of \code{"policy_tree"} or \code{"linear"},
or a list with \code{fit} and \code{predict} functions for a BYO
contextual policy.}

\item{best_arm}{One of \code{"ips"} or \code{"simple"}, or a BYO
\code{list(fit, predict)}.}

\item{reward_model}{One of \code{"random_forest"} or \code{"linear"},
or a BYO \code{list(fit, predict)}.}

\item{policy_features}{Optional integer vector of 1-based column indices
passed to the contextual policy. Defaults to all columns.}

\item{n_cores}{Number of parallel workers for the shuffle loop. Defaults
to 1 (sequential).}

\item{seed}{Base random seed. Shuffle s uses \code{seed + s}.}

\item{verbose}{If \code{TRUE}, emits progress messages.}
}
\value{
A \code{"kpe"} S3 object.
}
\description{
3-fold rotation of the centered IPW statistic from Eq. S18 of
Li & Brunskill (2026). No reward model is fit; the \code{reward_model}
argument is accepted for API symmetry with \code{\link{kpe}} and is
ignored. \code{n_folds} is also ignored -- PAPD always uses 3 folds.
}
