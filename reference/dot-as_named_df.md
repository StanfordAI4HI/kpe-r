# Coerce a feature matrix to a data frame with deterministic column names (f1, f2, ...), so ranger fit/predict align regardless of incoming colnames.

Coerce a feature matrix to a data frame with deterministic column names
(f1, f2, ...), so ranger fit/predict align regardless of incoming
colnames.

## Usage

``` r
.as_named_df(feat)
```
