# Extract the point estimate from a kpe fit.

Extract the point estimate from a kpe fit.

## Usage

``` r
# S3 method for class 'kpe'
coef(object, ...)
```

## Arguments

- object:

  A `"kpe"` object.

- ...:

  Ignored.
