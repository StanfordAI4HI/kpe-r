# Random-forest propensity via ranger probability forest.

Random-forest propensity via ranger probability forest.

## Usage

``` r
.propensity_forest(
  seed = NULL,
  num_trees = 100L,
  rf_threads = getOption("kpe.rf_threads", 1L)
)
```
