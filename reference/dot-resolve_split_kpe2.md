# Resolve the kpe2 fold split: m nuisance folds and eval-block size b = K - m. Validates that b divides K (non-overlapping evaluation blocks, "option A").

Resolve the kpe2 fold split: m nuisance folds and eval-block size b =
K - m. Validates that b divides K (non-overlapping evaluation blocks,
"option A").

## Usage

``` r
.resolve_split_kpe2(n_folds, n_nuisance_folds)
```
