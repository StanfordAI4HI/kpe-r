# Confidence interval for a kpe fit.

Confidence interval for a kpe fit.

## Usage

``` r
# S3 method for class 'kpe'
confint(object, parm, level = 0.95, ...)
```

## Arguments

- object:

  A `"kpe"` object.

- parm:

  Ignored (only one parameter, psi).

- level:

  Confidence level; defaults to 0.95.

- ...:

  Ignored.
