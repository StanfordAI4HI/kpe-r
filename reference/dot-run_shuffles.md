# Run the shuffle loop with optional parallelism via \`future\`.

Run the shuffle loop with optional parallelism via \`future\`.

## Usage

``` r
.run_shuffles(fn, seeds, n_cores = 1L, verbose = FALSE)
```
